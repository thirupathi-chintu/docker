version: 2.1
jobs:
  build:
    docker:
      - image: ubuntu
    steps:
      - checkout
      - run:
          name: "Testing"
          shell: /bin/sh
          command: |
            cd /tmp
            apt install git
            git clone https://github.com/thirupathi-p/terra.git
            cd terra
            bash wel.sh
      
          
